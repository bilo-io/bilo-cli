import React, { Component, PropTypes } from 'react';
import './style.scss';

export const MyComponent = (props) => {
    return (
        <div>
            <h1>MyComponent</h1>
        </div>
    )
}

export default MyComponent;